// @ts-check

import Heap from './heap';

/**
 * A MinHeap is a data structure that forms a kind of binary tree symbolically.
 * In it every parent node is smaller than this corresponding children.
 * At the root of the tree the element is the smallest value.
 * */
class MinHeap extends Heap {}

export default MinHeap;
